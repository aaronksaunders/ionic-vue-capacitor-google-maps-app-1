import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'map.vv',
  appName: 'map-vv',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
